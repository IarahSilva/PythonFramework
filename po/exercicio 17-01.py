class ControleRemoto:
    # Usamos 'pass' para indicar ao Python que a classe existe, 
    # mas ainda não tem regras ou dados internos definidos.
    pass 

# Instanciação: Criando o objeto na memória
controle_sala = ControleRemoto()